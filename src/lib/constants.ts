/** Milliseconds before the seed phrase is automatically hidden on the backup screen. */
export const SEED_AUTO_HIDE_MS = 30_000;

/** Seconds after which the clipboard is cleared following a seed phrase copy. */
export const SEED_CLIPBOARD_CLEAR_SECS = 60;
