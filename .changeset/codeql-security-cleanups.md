---
"sigil": patch
---

Tighten notification text sanitization and rewrite random crypto buffer generation to avoid false-positive hard-coded secret alerts in native storage and vault encryption paths.
