---
"sigil": patch
---

Add lastUsedAt to ApprovedDapp, cross-check verify-message signer, validate DONATION_IDENTITY at init, lock_clipboard only clears when sensitive content pending, warn on multi-account vault deletion, identity copy-on-tap in vault detail, SendToMany scroll hint
