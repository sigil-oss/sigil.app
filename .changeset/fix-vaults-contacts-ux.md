---
"sigil": patch
---

Show an error modal when importing a malformed vault file instead of silently ignoring it. When the active vault is deleted, automatically activate the next vault and navigate to the lock screen. Show last-used time on contact cards and sort contacts by recent use.
