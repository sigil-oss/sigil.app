---
"sigil": patch
---

Add autocomplete=current-password/new-password to all vault password inputs so password managers work correctly; add wheel and touchmove to auto-lock activity reset events; guard vaults-screen Enter key from concurrent vault unlock/import/delete
