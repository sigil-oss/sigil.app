---
"sigil": patch
---

Enforce exp field in deep-link parseEnvelope to reject stale requests on the frontend; decouple addVault from activeVaultId so setup screens explicitly control which vault becomes active
